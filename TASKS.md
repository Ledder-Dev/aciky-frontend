# TASKS — yoga-v2

## backlog
- [ ] [S] [infra][10] **Investigar caídas intermitentes aciky.org (530 / "response stream aborted")** — monitoreo externo detectó caídas el 2026-08-05 (varios ciclos Down/Up en minutos). Diagnóstico: dominio usa nameservers Cloudflare (`brett.ns.cloudflare.com`, `kia.ns.cloudflare.com`) vía Namecheap, pero zona no aparece en cuenta Cloudflare propia del usuario — probable integración "Cloudflare gratis" de Namecheap gestionada desde su propio panel, sin cuenta cloudflare.com separada. Fuera del repo (infra DNS), sin código que tocar acá. Pendiente: usuario revisar pestaña "Cloudflare" dentro de Namecheap (Domain List → Manage `aciky.org`) o intentar login en cloudflare.com con email de compra del dominio; una vez con acceso, verificar registros A (deben ser las 4 IPs de GitHub Pages: `185.199.108/109/110/111.153`) y modo SSL/TLS (debe ser "Full", no "Flexible"). (2026-08-07)

## doing

## review

## done
- [x] [P] [fix][13] **FAQ 404 en producción + fusiona link FAQ en dropdown "Nosotros"** — `vite.config.js` sin entry `faq` en `build.rollupOptions.input`, Vite build multi-página solo emite HTML registrado ahí (dev server lo enmascaraba). `header.html` desktop nav: "Nosotros" y "Preguntas Frecuentes" links sueltos apretaban la barra — fusionados en dropdown "Nosotros" (patrón hover existente de "Actividades"/"Galería"). Verificado en navegador. (2026-08-12)
- [x] [P] [feature][12] **Página FAQ (pages/faq.html) + links en header/footer** — 9 categorías, 36 preguntas (contenido del usuario), acordeón `<details>` nativo poblado dinámicamente vía `src/js/faq.js` desde `src/i18n/{es,en}/faq.json`. JSON-LD `FAQPage` inyectado en `<head>`, recalculado en cambio de idioma. Links agregados en `header.html` (desktop+mobile) y `footer.html`, entradas `header.faq`/`footer.faq` en `common.json`. Ruteo vía `main.js` `initPage()` siguiendo patrón existente. `npm run build` verificado sin errores. (2026-08-12)
- [x] [P] [fix][11] **Widget de chat: mover de index.html a footer partial (sitewide)** — solo salía en homepage porque el `<script>` de `widget.js` estaba hardcodeado en `index.html`; movido a `src/partials/footer.html`, sale ahora en las 47 páginas que incluyen `{{> footer}}`. Verificado en vivo (`aciky.org/pages/testimonials.html`, `/pages/blog.html`) tras deploy. Quedan sin widget por diseño (no bug): `pages/404.html` (error), `pages/membership.html` (documento imprimible, footer propio), `pages/admin/email-broadcast.html` (admin, sin partials header/footer) — confirmado con usuario dejar así. Commit `ca33eda`, pusheado a `origin/development`. (2026-08-11)
- [x] [P] [feature][9] **Widget de chat Supabase en homepage** — `index.html`: script `widget.js` (Supabase Storage) con `data-project-key`, `data-endpoint` (`/functions/v1/chat`), color `data-primary-color` ajustado a `#708558` (verde salvia, `--color-primary` de marca) tras primer valor `#e63946` (rojo, no combinaba). Commits `a8827ba`, `6f1d01a`, pusheados a `origin/development`. (2026-08-04)
- [x] [P] [content][8] **about.html: enriquecer historia con doc NKYTA + fotos por sección + logos colaboraciones clickeables** — Historia ampliada (fecha fundación 26 jun 2025, sección Stats, sección Colaboraciones) desde `NKYTA Spotlight ACIKY.docx`. Fotos añadidas: historia (`images/about/our-story.png`, banner completo `object-contain`), linaje (`images/about/yogi-bhajan.gif`, foto flotante a la derecha del texto, párrafos p1+p2 unidos en mismo contenedor pa eliminar espacio muerto), membership (`images/about/join-aciky.png`) y donate (`images/about/donation.png`), ambas banner completo sin recorte. Grid de 10 logos de colaboración (`images/about/logos/`) clickeables con `target="_blank"` a sitio web/red social de cada aliado, grid `grid-cols-8 sm:grid-cols-12` pa logos pequeños. 4 iteraciones de ajuste visual vía Chrome MCP (fotos recortadas → completas → tamaño → linaje con hueco → logos grandes → logos chicos). Commits `2218fba`, `28f9241`, `8c820aa`, `ea28fff`, `b5134da`, pusheados a `origin/development`. (2026-07-28)
- [x] [P] [seo][7] **Fix soft 404 en event.html (Search Console)** — `event.html?id=X` devolvía HTTP 200 con "Evento no encontrado" pa cualquier id inválido/inexistente, sin señal pa Google de que no había contenido real. `src/js/events.js`: nueva `setNoIndex()` inyecta `<meta name="robots" content="noindex">` solo en ausencia confirmada (sin id, o 404 explícito de API) — falla transitoria de fetch (ej. cold-start Heroku) no des-indexa evento real. Commit `fc450d2`, pusheado a `origin/development`, PR development→main pendiente de merge por el usuario (permiso MCP GitHub insuficiente pa crearlo). Diagnosticadas también otras 3 razones de GSC: robots.txt bloqueando `dashboard.html`/`admin/dashboard.html` (intencional, sin acción), `schedule.html`/`contact.html` en "Discovered/Crawled - currently not indexed" (backend responde rápido, no es bug de código — recomendado solo "Solicitar indexación" manual). (2026-07-28)
- [x] [P] [e2e][6] **Cobertura e2e: resto de páginas (registro, verify-email, forgot/reset-password, instructor/*, admin CRUD restante, público restante)** — `e2e/auth-flows.spec.ts` (registro con marcador `e2e-register-...@example.test` + sweep, verify-email 2 estados, forgot-password, reset-password sin token/token inválido); `e2e/instructor.spec.ts` (cuenta instructor real creada vía API, marcador `e2e-instructor-...@example.test` + sweep, 4 páginas instructor); `e2e/pages-smoke.spec.ts` (17 páginas admin/autenticadas + 15 públicas + event.html/404.html, data-driven por `h1[data-i18n]`). Bug real encontrado y corregido: `festival.js` crasheaba (`days.map is not a function`) si `program_json` no era array — ahora usa `Array.isArray`. 61/61 tests en verde, cero huérfanos verificados. (2026-07-25)
- [x] [P] [e2e][5] **Cobertura e2e: admin users CRUD** — `e2e/admin-users.spec.ts`: crear/editar/eliminar usuario (con `confirm()`) marcado por email único (`e2e-accountant-...@example.test`, dominio `.test` imposible de colisionar con cuenta real); recent registrations card verificada sin forzar mark-seen sobre datos reales. Sweep de seguridad `beforeAll`/`afterAll` por prefijo de email exacto. 17/17 tests en verde, cero huérfanos verificados. (2026-07-25)
- [x] [P] [e2e][4] **Cobertura e2e: accountant (transacciones, balance, exchange)** — `e2e/accountant.spec.ts`: resumen de fondos, CRUD manual (crear/editar/borrar con `confirm()`), conversión de moneda (par CUP/USD, borrado de ambos), filtro por tipo; marcadores únicos (`E2E Accountant ...`) en `category`/`description`, sweep de seguridad `beforeAll`/`afterAll` que solo coincide con el marcador exacto (nunca patrón genérico como "Conversión CUP"). 15/15 tests en verde, cero huérfanos verificados. (2026-07-25)
- [x] [P] [e2e][3] **Cobertura e2e: booking de clases (schedule.html)** — `e2e/booking.spec.ts`: redirect a login (logged out) y deep-link WhatsApp (autenticado); seed/cleanup propio vía `POST`/`DELETE /api/activities` (admin), self-healing (barre huérfanos de corridas previas antes de sembrar). 11/11 tests en verde. (2026-07-25)
- [x] [P] [e2e][2] **Correr suite e2e completa contra `yoga-backend` local** — `.env` poblado, backend levantado en `localhost:3000`, `npm run test:e2e` → 9 passed. (2026-07-25)
- [x] [P] [e2e][1] **Poblar `.env` con `E2E_ADMIN_EMAIL`/`E2E_ADMIN_PASSWORD`** — acción del usuario, credenciales de su cuenta admin real, no compartir por chat. (2026-07-25)
- [x] [P] [e2e][0] **Harness Playwright inicial** — `playwright test --list` OK, 9 tests en 4 archivos: `auth.setup.ts`, `login.spec.ts`, `admin-dashboard.spec.ts`, `public-pages.spec.ts`. Script `test:e2e`/`test:e2e:ui`, `.gitignore` cubre `.env` y `e2e/.auth/`. (2026-07-23)
