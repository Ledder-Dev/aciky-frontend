# TASKS — yoga-v2

## backlog
- [ ] [P] [infra][26] **Fondo blanco del ícono ACIKY en Dashy** — `.ico` ya es transparente (verificado, ver `[seo][25]`) y `aciky.org` muestra el favicon bien en el tab del navegador. En Dashy sigue con fondo blanco — sospecha: proxy `f1.allesedv.com` (resuelve `icon: favicon`) compone el ícono sobre lienzo blanco al servirlo, sin importar el alpha del `.ico` original. Alternativa no aplicada: cambiar el campo `icon` de ACIKY en `ladder/dashy/pages/Pagina_Principal.yml` de `favicon` a `direct-url` apuntando directo al `.ico`, como ya hacen `freemanstyle`/`studioalbor` — bypasea el proxy. Es config del repo `ladder`, no de este — usuario decidió no tocarlo por ahora ("no hagas nada"). (2026-09-04)

## doing

## review

## done
- [x] [P] [search][28] **Buscadores del sitio insensibles a tildes/acentos** — nueva utilidad compartida `normalizeText()` (`src/js/utils/normalize.js`, NFD + strip diacriticos via stdlib, sin mapa manual) aplicada a los 7 buscadores del sitio: publico (`posturas.js`, `videos.js`, `blog.js` — busqueda libre, `activeTag` filter dejado intacto) y admin (`admin/posturas.js`, `admin/videos.js`, `admin/users.js`, `admin/onlineSadhana.js`). `npm run build` verificado sin errores. Commit `9dbfe00`, PR #139 mergeado a `master`, `develop` sincronizada. (2026-09-06)
- [x] [P] [footer][27] **Credito "Sitio desarrollado por Ladder Dev" en footer sitewide** — link a ladderdev.com agregado en `src/partials/footer.html`, presente en las paginas que incluyen el partial. PR #138 mergeado a `master`. (2026-09-06)
- [x] [P] [seo][25] **Fondo blanco del favicon.ico (opaco en vez de transparente)** — PNG embebido en `public/favicon.ico` tenía colortype 2 (RGB, sin canal alpha), heredado del rasterizado original vía screenshot de Playwright/Chromium sin `omitBackground: true` (Chromium por defecto compone sobre lienzo blanco). SVG fuente (`public/images/logo/logo.svg`) confirmado sin fondo blanco propio — el defecto se introducía solo en la rasterización. Regenerado desde el SVG con `sharp` (instalado `--no-save`, ephemeral) preservando alpha, re-envuelto en el mismo contenedor ICO (ICONDIR+ICONDIRENTRY, w=64 h=64 bpp=32). Verificado: nuevo PNG reporta colortype 6 (RGBA); `npm run build` sin errores; `dist/favicon.ico` idéntico byte a byte a `public/favicon.ico`. Commit `ac15142`, PR #136 mergeado a `master`. Usuario confirmó bien en `aciky.org` (tab del navegador) — Dashy sigue con fondo blanco por causa distinta, ver `[infra][26]`. (2026-09-04)
- [x] [P] [infra][24] **Forzar refresh del icono ACIKY en Dashy** — tile usa `icon: favicon`, resuelto vía proxy externo `f1.allesedv.com` que cachea el favicon por dominio server-side. Root cause: proxy lee el primer `<link rel="icon">` del HTML, no rasteriza SVG ni cae al segundo tag — HTML tenía SVG primero, `.ico` segundo. Corregido orden (`.ico` primero) en las 51 páginas, PR #135 mergeado a `master` (2026-09-04), deploy confirmado, usuario reportó "ya funciona". (2026-09-04)
- [x] [S] [infra][10] **Investigar caídas intermitentes aciky.org (530 / "response stream aborted")** — usuario tomó control directo del dominio en Cloudflare (nameservers migrados de Namecheap a Cloudflare propio), administra la zona el mismo ahora. Sin diagnóstico puntual confirmado de la causa raíz original; usuario reporta resuelto tras el cambio. (2026-09-04)
- [x] [P] [seo][23] **favicon.ico faltante en producción + declaración explícita en `<link rel="icon">`** — `public/` solo tenía `logo.svg` (SVG-only `<link>`); tile de ACIKY en Dashy (`icon: favicon`) mostraba icono roto. Root cause en 2 capas: (1) faltaba `public/favicon.ico` — generado rasterizando el SVG (Playwright/Chromium, data URI base64, 64x64), primer intento guardó bytes PNG crudos renombrados `.ico` (servía bien en navegador por content-type/extension, pero parsers de magic bytes lo rechazaban) — corregido envolviéndolo en contenedor ICO real (header ICONDIR/ICONDIRENTRY), mismo formato que `worlds/ladder-web/public/favicon.ico`; (2) las 51 páginas HTML solo declaraban `<link rel="icon" type="image/svg+xml">` sin fallback `.ico` explícito — el proxy de favicons que usa Dashy (`allesedv.com`) lee el `<link>` del HTML, no rasteriza SVG ni hace fallback implícito a `/favicon.ico` como un navegador real; agregado `<link rel="icon" type="image/x-icon" href="favicon.ico">` en las 51 páginas, mismo patrón que `worlds/freemanstyle` (que sí funciona en Dashy). Cloudflare edge cacheaba versión vieja del favicon tras el primer deploy — usuario purgó cache manual desde dashboard (ya migrado de Namecheap a Cloudflare directo). Commits `0067dd2`+`5c2ca72`+`41999ce`, PRs #127-132 mergeados `devRandy`→`develop`→`main`, verificado en vivo (`curl -I`, magic bytes, `<link>` tag). Sigue pendiente refresh del cache del proxy de Dashy — ver `[infra][24]`. (2026-09-03)
